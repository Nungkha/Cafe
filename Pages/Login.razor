@page "/loginrazor"
@using Cafe.Data.Models


<h3>Login</h3>

<EditForm Model="@loginModel" OnInvalidSubmit="Submit">
	<DataAnnotationsValidator />
	<div class="form-group">
		<input type="email" @bind-value="loginModel.Email" class="form-control" placeholder="Email address" />
		<ValidationMessage For="@(()=> loginModel.Email)" />
	</div>

	<div class="mt-2 form-group">
		<input type="password" @bind-value="loginModel.Password" class="form-control" placeholder="Password" />
		<ValidationMessage For="@(()=> loginModel.Password)" />
	</div> 


	<div class="mt-2 text-end">
		<NavLink class="nav-link" href="registration">
			<span>Don't have an account? Register</span>
		</NavLink>
	</div>

	<div class="text-center mt-2">
		<button class="btn btn-primary" type="submit">Login</button>
	</div>
</EditForm>

@code {
	// creating obj of LoginModel
	LoginModel loginModel = new();

	private void Submit()
	{
		
	}

}
